<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%),  <% if @tool.photo? %>
            url('<%= cl_image_path tool.photo, height: 250, width: 300, crop: :fill, gravity: "faces" %>')
          <% else %>
            url('https://picsum.photos/300/200/?random')
<% end %>">
</div>

<h2> <%= @tool.name %></h2>
<h3><%=  @tool.description %></h>



<%= link_to "Delete", tool_path(@tool), method: :delete %>

<%= link_to "Return", tools_path %>

<%=render 'shared/form' %>


